git --version --> checks the current version of git

git config --global user.name "Your Name"  -->  configures your name 
git config --global user.email "your@email.com" --> configures your email

git init --> intializes a local repo into a folder (.git folder is created)

git status --> tracks changes of the local repo i.e shows files modified or deleted or added

git add . --> adds all the files of the current folder into the staging area
git add file name --> adds the particular file into the staging area

git commit -m "Message"  --> creates a checkpoint which can be visited again using git chekout in case something goes wrong.

git restore --staged . --> removes all the files from the staging area
git restore --staged filename --> removes the particular file from the staging area 

git reset --> moves all the staged files to unstaged state, similar to git restore 

Now the thing is that  
git push
git push -u origin main -- helps in configuring push directly using git push and not git push origin main

origin refers to the link of the remote repo